# Public configuration, deliberately committed.
#
# Both values below already reach every visitor's browser on any Supabase
# app — access control lives in the database's row level security, not in
# the secrecy of these strings.
#
# SECRETS DO NOT BELONG IN THIS FILE. The service-role key bypasses row
# security and must only ever be set in the hosting dashboard:
#   Vercel -> cleenzy -> Settings -> Environment Variables
#     SUPABASE_SERVICE_ROLE_KEY = (Supabase -> Settings -> API, secret key)
# Booking (guest orders) does not work until that key is set.
# Values set in the Vercel dashboard override this file.

NEXT_PUBLIC_SUPABASE_URL=https://oyqbfljqmuasshwrabua.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_sY1ZaqRfyYb5FIcWoglcqw_lQuVJDNX
NEXT_PUBLIC_SITE_URL=https://cleenzy.vercel.app
